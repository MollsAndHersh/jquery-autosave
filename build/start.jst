(function( factory ) {

	// AMD
	if ( typeof define === "function" && define.amd ) {
		define( [ "jquery", "fixture" ], factory );

	// Browser
	} else {
		factory( jQuery, Fixture );
	}

})(function( $, Fixture ) {
